# Changed: LLM 기반 solver — torch/transformers/accelerate 의존성 추가.
# Why: Qwen3.5-0.8B full FT merged model 로드에 필요.
[project]
name = "team6-opal-verifier"
version = "0.3.0"
description = "LLM-based SSD TCG/Opal command-response trajectory verifier for Team 6"
requires-python = "==3.12.3"
dependencies = [
    "torch",
    "transformers",
    "accelerate",
]

[tool.ruff]
line-length = 100
target-version = "py310"
